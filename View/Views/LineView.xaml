<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="Views.LineView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:BaseballScoreView"
    xmlns:vm="clr-namespace:BaseballScoreHelper.ViewModels;assembly=WpfHelper"
    mc:Ignorable="d"
    Title="Main Window"
    Height="672" Width="840"
>
  <Window.DataContext>
    <vm:VictoryLineViewModel />
  </Window.DataContext>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition  Height="Auto" />
      <RowDefinition  Height="*" />
    </Grid.RowDefinitions>

    <TabControl
        Grid.Row="0"
        Margin="8,8,8,8"
    >
      <TabControl.ItemTemplate>
        <DataTemplate>
          <TextBlock  Text="LeagueName" />
        </DataTemplate>
      </TabControl.ItemTemplate>
    </TabControl>

    <Grid
        Grid.Row="1"
        Margin="8,0,8,8"
    >
      <Grid.ColumnDefinitions>
        <ColumnDefinition  Width="*" />
        <ColumnDefinition  Width="Auto" />
      </Grid.ColumnDefinitions>
      <DataGrid
          Grid.Column="0"
          AutoGenerateColumns="True"
          CanUserAddRows="False"
          RowHeaderWidth="0"
          Margin="8,8,8,0"
          IsReadOnly="True"
          ItemsSource="{Binding LineDataTable}"
      >
        <DataGrid.RowStyle>
          <Style TargetType="DataGridRow">
            <Setter Property="HorizontalAlignment" Value="Left" />
          </Style>
        </DataGrid.RowStyle>
      </DataGrid>
      <Calendar  Grid.Column="1"  Margin="8,8,8,0" />
    </Grid>
  </Grid>

</Window>
